version: "3.9"

services:
  Dev-Flask-API:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../models:/models:rw
      - ../views:/views:rw
      - ../logs:/logs:rw
    ports:
      [
        5000:5000
      ]
    networks:
      - dev-network
    command: python3 run.py
    stdin_open: true
    tty: true

networks:
  dev-network:
    driver: bridge